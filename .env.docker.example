# Docker 部署配置 —— 复制为 .env.docker 并填真值（.env.docker 已加进 .gitignore）
#   cp .env.docker.example .env.docker
#   docker compose --env-file .env.docker up -d --build
# 说明：仓库自身 .env 是单条 PG_CONNECTION_STRING（指向 127.0.0.1），Docker 不用它；
#       db 服务与 api 的 DSN 都由这里同一个 PG_SUPERPASSWORD 拼出。示例值仅占位，无真密钥。

# ---- 数据库（唯一密码来源）----
PG_SUPERUSER=postgres
PG_SUPERPASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
PG_DB=cnc_kb
PG_PORT=5432

# ---- 运行 ----
TZ=Asia/Shanghai
APP_ENV=Production
BACKEND_PORT=8000      # 宿主机端口
REGISTRY=              # 留空本地构建；上线填如 yourrepo/
TAG=latest

# ---- 远程 AI（真实问答/向量需填）----
DEEPSEEK_API_KEY=sk-REPLACE
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
SILICONFLOW_API_KEY=sf-REPLACE
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DIM=1024
RERANK_MODEL=BAAI/bge-reranker-v2-m3
RERANK_THRESHOLD=0.30

# ---- 鉴权 ----
JWT_SECRET=REPLACE_WITH_RANDOM_AT_LEAST_32_CHARS
